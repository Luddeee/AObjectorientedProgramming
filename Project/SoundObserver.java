package Project;

// This code snippet is defining a Java interface named `SoundObserver`. This interface has one method
// signature `playSoundNotification()` which does not have any implementation details. Any class that
// implements this interface will need to provide an implementation for the `playSoundNotification()`
// method. This interface can be used to define a contract for classes that need to receive sound
// notifications.
public interface SoundObserver {
    void playSoundNotification();
}

